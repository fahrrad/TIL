# threading modules implements concurrency through application threads
# multiprocessing uses system processes
# asyncio uses single thread, single process approach 

# Coroutines are designed for concurrent operation
